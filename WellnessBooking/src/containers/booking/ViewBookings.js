import React, { Component } from 'react';
import { View, Text } from 'react-native';

class viewBookings extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> viewBookings </Text>
      </View>
    );
  }
}

export default viewBookings;
